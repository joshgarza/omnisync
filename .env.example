# Create a .env file from this template and fill in the values.
# ----------------------------------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# These values are used to configure the 'db' service in docker-compose.yml
# The DATABASE_URL is automatically derived from these values within the 'backend' service.
# ----------------------------------------------------------------------
POSTGRES_USER=omnisync_user
POSTGRES_PASSWORD=omnisync_secret
POSTGRES_DB=omnisync_db

# ----------------------------------------------------------------------
# SERVICE PORTS
# These variables control which ports the services run on.
# ----------------------------------------------------------------------
BACKEND_PORT=8080
FRONTEND_PORT=5173